# Transferencia a otra PC

La fuente de verdad del proyecto es GitHub (`main`). Los despliegues de producción se realizan desde Vercel y los datos operativos se guardan online en Supabase.

Para continuar desde otra computadora, clonar el repositorio, instalar dependencias y actualizar `main` antes de modificar.
